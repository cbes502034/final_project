# ============================================================
# Render 部署設定
# ------------------------------------------------------------
# 兩個服務 + 一個資料庫：
#   web  靜態前端（走 CDN，即時載入）
#   api  FastAPI 後端
#   db   PostgreSQL
# ============================================================

databases:
  - name: fambudget-db
    plan: free
    databaseName: fambudget
    user: fambudget

services:
  # ---------- 前端：純靜態 ----------
  - type: web
    name: llm-capstone-top20
    runtime: static
    buildCommand: ""
    # ⚠️ 目錄名稱是 site 不是 frontend —— 這不是筆誤。
    # Render 上這個服務是用後台建立的，發布目錄寫死成 site，
    # 而且這份 blueprint 目前沒有被套用（fambudget-api 服務並不存在）。
    # 改資料夾名稱會讓建置失敗，站台會停在上一次成功的版本。
    # 要改名的步驟寫在 README 的「部署」那一節。
    staticPublishPath: ./site
    pullRequestPreviewsEnabled: false
    routes:
      # 前端走 hash 路由，任何路徑都回 index.html
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: Referrer-Policy
        value: strict-origin-when-cross-origin
      - path: /*
        name: X-Frame-Options
        value: SAMEORIGIN

  # ---------- 後端：FastAPI ----------
  - type: web
    name: fambudget-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
      - key: DATABASE_URL
        fromDatabase:
          name: fambudget-db
          property: connectionString
      - key: ALLOWED_ORIGINS
        value: https://llm-capstone-top20.onrender.com,http://localhost:5174
      # 下面兩個要在 Render 後台自行填入，不要寫進 repo
      - key: JWT_SECRET
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
