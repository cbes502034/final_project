"""
段落記帳的請求與回應模型。 ✦ 負責人：成員2（段落記帳）　✦ 分支：m2-ledger

===========================================================================
這個檔案要定義哪些模型
===========================================================================
    ParseRequest             text
    ParsedItem               seq、span、date、amount、kind、cat、merchant、conf、missing、hint
    ParseBatchResponse       raw、items、note
    ConfirmRequest           raw、parsed、corrected、parse_id

===========================================================================
models/ 和 schemas/ 的差別
===========================================================================
    models/    資料庫裡存什麼    User 有 password_hash
    schemas/   API 收送什麼      UserResponse **沒有** password_hash

分開的好處立刻看得到：你**不可能「不小心」把密碼雜湊回傳給前端**，
因為 response model 裡根本沒有那一欄。

===========================================================================
回應形狀要跟前端對得上
===========================================================================
`frontend/js/api.js` 裡有完整的路由契約，而且 mock 模式已經用假資料
把所有畫面跑起來了。**不確定要回什麼形狀時，去看 api.js，那是唯一的真相來源。**

⚠️ `ParsedItem` 這個模型有兩個用途，**兩個都很重要**：

1. 回給前端顯示
2. **拿來驗證模型的輸出** —— 模型回傳的 JSON 一定要過這個模型的驗證才採用

第 2 點是「強制結構化輸出」的實作。驗不過就重試，
重試仍失敗才回錯誤給前端。

`missing` 一定要準。寧可回報缺漏讓使用者填，也不要猜一個數字填進去——
**猜錯的金額比空白危險得多**，空白使用者一定會發現，猜錯的他可能直接按下確認。
"""

# TODO(成員2): 定義上面列的 Pydantic 模型
