"""
模型評測。 ✦ 負責人：成員4（家庭與可見範圍）　✦ 分支：m4-access

===========================================================================
這是整個專題的量化成果
===========================================================================
報告裡最有說服力的東西不是「我們做了一個記帳系統」，
而是**一張零樣本 vs few-shot 的對照表**。這個檔案負責產出那張表。

===========================================================================
為什麼評測不歸寫模型的人？
===========================================================================
成員2 寫 prompt，如果評測也由他做，等於自己考自己。
交給另一個人做，數字才有可信度。這在業界叫獨立驗證。

而且成員2 在第 2 週會很忙（prompt 是整個專題最難的部分），
評測交給成員4 剛好可以平行進行。

===========================================================================
⚠️ 留出集必須 100% 人工標註
===========================================================================
這是最容易踩到、而且最致命的一條。

如果測試集是商業模型標的，評測量到的就變成
**「我們的模型有多像那個老師」**，而不是「我們的模型有多正確」。
老師錯的地方，我們學得越像分數越高 —— **整張對照表會變成假的**。

訓練資料可以用商業模型產候選標註再由人修正（那是常規做法），
**但留出集不行，一筆都不行。**

===========================================================================
四個指標
===========================================================================
    一次輸入完全正確率   五個欄位全對才算成功。這是主要指標
    切分正確率           切出的筆數與人工標註一致
    分類 Macro-F1        各分類的 F1 取平均，避免被大類別的高分蓋過
    零樣本 vs few-shot   同一份測試集跑兩種 prompt 的對照
"""

from typing import Any

from sqlalchemy.orm import Session


def build_holdout(db: Session) -> list[dict[str, Any]]:
    """
    從 nlp_parses 撈出候選，整理成人工標註用的清單。

    ⚠️ 撈出來之後**必須由人逐筆檢查**，確認標註正確才放進留出集。
    直接拿模型的輸出當答案，等於用模型考自己。

    TODO(成員4): 實作。建議至少 100 筆，涵蓋所有分類，
                 而且要刻意包含那些「規則做不到」的難題：
                 店名歧義、收支方向、合計語意、相對日期
    """
    raise NotImplementedError("TODO：成員4 尚未實作")


def exact_match_rate(predictions: list[dict], truth: list[dict]) -> float:
    """
    一次輸入完全正確率。**五個欄位全對才算一次成功。**

    為什麼用這個當主要指標？因為單看個別欄位的分數會高估實際體驗——
    使用者在意的是「我寫一句話，它有沒有**一次**記對」，
    只要有一欄錯就要動手改，摩擦就回來了。

    TODO(成員4): 實作
    """
    raise NotImplementedError("TODO：成員4 尚未實作")


def category_macro_f1(predictions: list[dict], truth: list[dict]) -> float:
    """
    分類的 Macro-F1：各分類各自算 F1 再取平均。

    為什麼不用準確率？因為「餐飲」這種大類別會把分數拉高，
    掩蓋掉小類別（醫療、教育）其實完全分不出來的事實。

    TODO(成員4): 實作
    """
    raise NotImplementedError("TODO：成員4 尚未實作")


def compare(db: Session) -> dict[str, Any]:
    """
    產出報告用的對照表：同一份留出集，零樣本 vs few-shot。

    TODO(成員4): 跑兩次評測並整理成表格資料
    """
    raise NotImplementedError("TODO：成員4 尚未實作")
